$I18N_comparePackageStatus<!--new 17.1 -->

Mit diesem Dialog können Sie den Paketstatus von zwei Clients miteinander vergleichen. Wählen Sie zum Vergleichen unter <i>"$I18N_clientOrFileSelection1"</i> und <i>"$I18N_clientOrFileSelection2"</i> die Datenquellen für die linke und rechte Spalte aus. Sollten Sie diesen Dialog über <i>"$I18N_overview_clients"</i> aufgerufen haben, so wurden die ersten beiden dort angehakten Clients vorausgewählt. Möchten Sie stattdessen andere Clients vergleichen, so wählen Sie diese aus der entsprechenden Liste aus und klicken Sie auf <i>"$I18N_useClientName"</i>. Möchten Sie eine zuvor gespeicherte Paketstatusdatei verwenden, so wählen Sie diese aus und laden Sie diese hoch. Ein Vergleich ist zwischen zwei Clients, zwei Paketstatusdateien oder einem Client und einer Paketstatusdatei möglich.

Zum Starten des Vergleiches klicken Sie abschließend auf <i>"$I18N_comparePackageStatus"</i>.

Beim Vergleichen werden hierbei Paketname, Versionsnummer und Installationsstatus berücksichtigt. Diese Informationen werden, sofern sie vorliegen, jeweils in den Spalten <i>"$I18N_package_name"</i>, <i>"$I18N_version"</i> und <i>"$I18N_status"</i> aufgelistet. In der Spalte <i>"&lt;=|&gt;"</i> sehen sie zudem, ob das Paket ausschließlich im linken (<i>"&lt;"</i>) oder rechten (<i>"&gt;"</i>) Paketstatus vorhanden ist oder keine Übereinstimmung gefunden werden konnte (<i>"|"</i>). Sollte das Paket auf beiden Seiten vorhanden sein (<i>"="</i>), so werden zusätzlich die Versionsnummern und Installationszustände abgeglichen. Gefundene Unterschiede werden farblich hervorgehoben. Eine Zusammenfassung des Vergleiches sehen Sie am Ende der Tabelle.

