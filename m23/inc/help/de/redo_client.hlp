$I18N_redo_client_jobs<!--new v12.4-->
Diese Seite wiederholt alle Aufträge (inklusive Partitionierung und Formatierung), die jemals auf dem m23-Client ausgeführt wurden. Anders als bei <i>"$I18N_recover_client"</i> wird kein zusätzlicher Auftrag generiert, der sicherstellt, daß alle Pakete, die zuvor auf dem m23-Client installiert waren, erneut installiert werden.