$I18N_currentWorkPHP
Auf dieser Seite sehen Sie das aktuelle Skript, das auf dem m23-Client als nächstes (oder gerade) ausgeführt wird sowie die Befehlszeile zum Abfragen des Skriptes.

Aus dem Textfeld neben <i>"$I18N_wgetWorkPHPGettingCode"</i> können Sie den Befehl kopieren, um das Skript abzufragen und anzeigen zu lassen. Diese Befehlszeile können Sie einfach in eine Linux-Shell ausführen.

Unter <i>"$I18N_currentWorkPHPContents"</i> sehen Sie den (farblich hervorgehobenen) Quelltext des Skriptes.