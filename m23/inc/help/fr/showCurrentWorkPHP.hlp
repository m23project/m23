$I18N_currentWorkPHP
Sur cette page, vous voyez le script actuellement exécuté sur le poste client m23 (ou qui sera le prochain à être exécuté) et la commande pour le téléchargement de ce script.

Vous pouvez copier la commande depuis le champs texte à côté de <i>"$I18N_wgetWorkPHPGettingCode"</i> pour pouvoir télécharger et afficher le script. Vous pouvez simplement exécuter cette commande dans un Shell Linux.

Sous <i>"$I18N_currentWorkPHPContents"</i> vous voyez le code source du script affiché selon une syntaxe colorée.
