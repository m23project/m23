$I18N_redo_client_jobs<!--new v12.4-->
Cette fonction vous permet de re-exécuter toutes les tâches que m23 a jamais exécuté sur le poste client (incluant le partitionnement et le formatage). La fonction ne crée <b>pas de tâche d'installation supplémentaire</b> (comme la fonction <i>"$I18N_recover_client"</i>), qui assurerait que tous les paquets qui ont étés installés auparavant seraient re-installés.
