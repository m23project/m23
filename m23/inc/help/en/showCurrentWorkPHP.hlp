$I18N_currentWorkPHP
At this page, you can see the script which is currently being executed on the m23 client (or which is the next one to be executed) as well as the command to request the script.

You can copy the command from the text field next to <i>"$I18N_wgetWorkPHPGettingCode"</i> to request and display the script. This command can simply be executed from a Linux shell.

Under <i>"$I18N_currentWorkPHPContents"</i>, you can see the source code of the script (with the syntax highlighted).
