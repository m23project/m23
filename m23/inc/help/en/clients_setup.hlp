Setup client
You can see the list of clients that are ready for partitioning and formatting. These clients have completed the hardware detection sequence.

To partition and format the client click on <i>"$I18N_setup"</i>.

helpInclude("statusColors.inc");

