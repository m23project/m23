
Template: libdvd-pkg/first-install
Type: note
Description:
 This package automates the process of launching downloads of the source
 files for ${PKGG} from videolan.org, compiling them, and installing the
 binary packages (${PKGG_ALL}).
 .
 Please run "sudo dpkg-reconfigure ${PKGI}" to launch this process for
 the first time.
Description-cs.UTF-8:
 Tento balíček automatizuje proces spouštění stahování zdrojových souborů pro ${PKGG} z videolan.org, kompiluje je a instaluje binární balíčky (${PKGG_ALL}).
 .
 Pro spuštění tohoto procesu nejprve prosím spusťte příkaz "sudo dpkg-reconfigure ${PKGI}".
Description-da.UTF-8:
 Denne pakke automatiserer processen med at starte overførsler af kildefilerne for ${PKGG} fra videolan.org, kompilere dem og installere de binære pakker (${PKGG_ALL}).
 .
 Kør venligst »sudo dpkg-reconfigure ${PKGI}« for at starte denne proces for den første gang.
Description-de.UTF-8:
 Dieses Paket automatisiert den Prozess, Quelldateien für ${PKGG} von »videolan.org« herunterzuladen, sie zu kompilieren und die Binärpakete (${PKGG_ALL}) zu installieren.
 .
 Bitte führen Sie »sudo dpkg-reconfigure ${PKGI}« aus, um diesen Prozess erstmalig in Gang zu setzen.
Description-fr.UTF-8:
 Ce paquet automatise les processus de téléchargement des fichiers sources pour ${PKGG} depuis videolan.org, la compilation et l'installation des paquets binaires (${PKGG_ALL}).
 .
 Veuillez exécuter « sudo dpkg-reconfigure ${PKGI} » pour lancer le processus la première fois.
Description-it.UTF-8:
 Questo pacchetto automatizza il processo di scaricamento di file sorgenti per ${PKGG} da videolan.org, la loro compilazione e l'installazione dei pacchetti binari (${PKGG_ALL}).
 .
 Eseguire «sudo dpkg-reconfigure ${PKGI}» per avviare questo processo per la prima volta.
Description-nl.UTF-8:
 Dit pakket automatiseert het proces van het ophalen bij videolan.org van de bronbestanden van ${PKGG}, het compileren ervan en het installeren van de binaire pakketten (${PKGG_ALL}).
 .
 Voer de opdracht "sudo dpkg-reconfigure ${PKGI}" uit om een eerste maal dit proces te starten.
Description-pt.UTF-8:
 Este pacote automatiza o processo de lançar downloads de ficheiros fonte para ${PKGG} a partir de videolan.org, compilando-os, e instalando os pacotes binários (${PKGG_ALL}).
 .
 Por favor corra "sudo dpkg-reconfigure ${PKGI}" para lançar este processo pela primeira vez.
Description-ru.UTF-8:
 Этот пакет автоматизирует процесс скачивания файлов исходного кода ${PKGG} с videolan.org, его компиляции и установки получившихся двоичных пакетов (${PKGG_ALL}).
 .
 Чтобы первый раз запустить данный процесс, выполните команду «sudo dpkg-reconfigure ${PKGI}».

Template: libdvd-pkg/title_b-i
Type: title
Description: Download, build and install ${PKGG}${VER}
Description-cs.UTF-8: Stáhnout, sestavit a nainstalovat ${PKGG}${VER}
Description-da.UTF-8: Hent, byg og installer ${PKGG}${VER}
Description-de.UTF-8: ${PKGG}${VER} herunterladen, bauen und installieren
Description-fr.UTF-8: Téléchargement, compilation et installation ${PKGG}${VER}
Description-it.UTF-8: Scaricare, compilare e installare ${PKGG}${VER}
Description-nl.UTF-8: Downloaden, bouwen en installeren van ${PKGG}${VER}
Description-pt.UTF-8: Descarregar, compilar e instalar ${PKGG}${VER}
Description-ru.UTF-8: Скачивание, сборка и установка ${PKGG}${VER}

Template: libdvd-pkg/build
Type: boolean
Default: true
Description: Download, build, and install ${PKGG}${VER}?
 This package automates the process of launching downloads of the source
 files for ${PKGG} from videolan.org, compiling them, and installing the
 binary packages (${PKGG_ALL}).
 .
 Please confirm whether you wish this to happen.
Description-cs.UTF-8: Stáhnout, sestavit a nainstalovat ${PKGG}${VER}?
 Tento balíček automatizuje proces spouštění stahování zdrojových souborů pro ${PKGG} z videolan.org, kompiluje je a instaluje binární balíčky (${PKGG_ALL}).
 .
 Potvrďte prosím, zda si přejete tuto akci provést.
Description-da.UTF-8: Hent, byg og installer ${PKGG}${VER}?
 Denne pakke automatiserer processen med at starte overførsler af kildefilerne for ${PKGG} fra videolan.org, kompilere dem og installere de binære pakker (${PKGG_ALL}).
 .
 Bekræft venligst om du ønsker at dette sker.
Description-de.UTF-8: ${PKGG}${VER} herunterladen, bauen und installieren?
 Dieses Paket automatisiert den Prozess, Quelldateien für ${PKGG} von »videolan.org« herunterzuladen, sie zu kompilieren und die Binärpakete (${PKGG_ALL}) zu installieren.
 .
 Bitte bestätigen Sie, dass Sie dies wünschen.
Description-fr.UTF-8: Faut-il télécharger, compiler puis installer ${PKGG}${VER} ?
 Ce paquet automatise les processus de téléchargement des fichiers sources pour ${PKGG} depuis videolan.org, la compilation et l'installation des paquets binaires (${PKGG_ALL}).
 .
 Veuillez confirmer que c'est bien ce que vous souhaitez faire ?
Description-it.UTF-8: Scaricare, compilare e installare ${PKGG}${VER}?
 Questo pacchetto automatizza il processo di scaricamento di file sorgenti per ${PKGG} da videolan.org, la loro compilazione e l'installazione dei pacchetti binari (${PKGG_ALL}).
 .
 Confermare che questo è ciò che si desidera.
Description-nl.UTF-8: ${PKGG}${VER} downloaden, bouwen en installeren?
 Dit pakket automatiseert het proces van het ophalen bij videolan.org van de bronbestanden van ${PKGG}, het compileren ervan en het installeren van de binaire pakketten (${PKGG_ALL}).
 .
 Bevestig dat u wilt dat dit gedaan wordt.
Description-pt.UTF-8: Descarregar, compilar, e instalar ${PKGG}${VER}?
 Este pacote automatiza o processo de lançar downloads de ficheiros fonte para ${PKGG} a partir de videolan.org, compilando-os, e instalando os pacotes binários (${PKGG_ALL}).
 .
 Por favor confirme se deseja que isto seja feito.
Description-ru.UTF-8: Скачать, собрать и установить ${PKGG}${VER}?
 Этот пакет автоматизирует процесс скачивания файлов исходного кода ${PKGG} с videolan.org, его компиляции и установки получившихся двоичных пакетов (${PKGG_ALL}).
 .
 Подтвердите, что хотите, чтобы всё это выполнилось.

Template: libdvd-pkg/title_u
Type: title
Description: Upgrade available for ${PKGG}
Description-cs.UTF-8: Dostupná aktualizace pro ${PKGG}
Description-da.UTF-8: Opgradering er tilgængelig for ${PKGG}
Description-de.UTF-8: Für ${PKGG} ist ein Upgrade verfügbar.
Description-fr.UTF-8: Nouvelle version disponible pour ${PKGG}
Description-it.UTF-8: Aggiornamento disponibile per ${PKGG}
Description-nl.UTF-8: Er is een opwaardering mogelijk van ${PKGG}
Description-pt.UTF-8: Actualização disponível para ${PKGG}
Description-ru.UTF-8: Для ${PKGG} доступно обновление

Template: libdvd-pkg/upgrade
Type: note
Description:
 This package automates the process of launching downloads of the source
 files for ${PKGG} from videolan.org, compiling them, and installing the
 binary packages (${PKGG_ALL}).
 .
 An update to version ${VER} is available, but automatic upgrades are
 disabled.
 .
 Please run "sudo dpkg-reconfigure ${PKGI}" to launch this process
 manually and/or activate automatic upgrades in future.
Description-cs.UTF-8:
 Tento balíček automatizuje proces spouštění stahování zdrojových souborů pro ${PKGG} z videolan.org, kompiluje je a instaluje binární balíčky (${PKGG_ALL}).
 .
 Je dostupná aktualizace na verzi ${VER}, automatická instalace aktualizací je však zakázána.
 .
 Pro ruční spuštění tohoto procesu spusťte prosím příkaz "sudo dpkg-reconfigure ${PKGI}" anebo do budoucna aktivujte automatickou instalaci aktualizací.
Description-da.UTF-8:
 Denne pakke automatiserer processen med at starte overførsler af kildefilerne for ${PKGG} fra videolan.org, kompilere dem og installere de binære pakker (${PKGG_ALL}).
 .
 En opdatering til version ${VER} er tilgængelig, men automatiske opgraderinger er deaktiveret.
 .
 Kør venligst »sudo dpkg-reconfigure ${PKGI}« for at starte denne proces manuelt og/eller aktiver automatiske opgraderinger i fremtiden.
Description-de.UTF-8:
 Dieses Paket automatisiert den Prozess, Quelldateien für ${PKGG} von »videolan.org« herunterzuladen, sie zu kompilieren und die Binärpakete (${PKGG_ALL}) zu installieren.
 .
 Eine Aktualisierung auf Version ${VER} ist verfügbar, automatische Upgrades sind jedoch ausgeschaltet.
 .
 Bitte führen Sie »sudo dpkg-reconfigure ${PKGI}« aus, um diesen Prozess manuell zu starten und/oder aktivieren Sie zukünftig automatische Upgrades.
Description-fr.UTF-8:
 Ce paquet automatise les processus de téléchargement des fichiers sources pour ${PKGG} depuis videolan.org, la compilation et l'installation des paquets binaires (${PKGG_ALL}).
 .
 Une mise à jour vers la version ${VER} est disponible, mais les montées de version automatiques sont désactivées.
 .
 Veuillez exécuter « sudo dpkg-reconfigure ${PKGI} » pour lancer ce processus manuellement et/ou activer les futures mises à niveau automatiques.
Description-it.UTF-8:
 Questo pacchetto automatizza il processo di scaricamento di file sorgenti per ${PKGG} da videolan.org, la loro compilazione e l'installazione dei pacchetti binari (${PKGG_ALL}).
 .
 È disponibile un aggiornamento alla versione ${VER}, ma gli aggiornamenti automatici sono disabilitati.
 .
 Eseguire «sudo dpkg-reconfigure ${PKGI}» per avviare questo processo manualmente o attivare gli aggiornamenti automatici per il futuro.
Description-nl.UTF-8:
 Dit pakket automatiseert het proces van het ophalen bij videolan.org van de bronbestanden van ${PKGG}, het compileren ervan en het installeren van de binaire pakketten (${PKGG_ALL}).
 .
 Er is een opwaardering naar versie ${VER} mogelijk, maar het automatisch opwaarderen staat uit.
 .
 Voer de opdracht "sudo dpkg-reconfigure ${PKGI}" uit om dit proces handmatig te starten of om een automatische opwaardering in de toekomst te activeren.
Description-pt.UTF-8:
 Este pacote automatiza o processo de lançar downloads de ficheiros fonte para ${PKGG} a partir de videolan.org, compilando-os, e instalando os pacotes binários (${PKGG_ALL}).
 .
 Está disponível uma actualização para a versão ${VER}, mas as actualizações automáticas estão desactivadas.
 .
 Por favor corra "sudo dpkg-reconfigure ${PKGI}" para lançar este processo manualmente e/ou activar as actualizações automáticas no futuro.
Description-ru.UTF-8:
 Этот пакет автоматизирует процесс скачивания файлов исходного кода ${PKGG} с videolan.org, его компиляции и установки получившихся двоичных пакетов (${PKGG_ALL}).
 .
 Доступна новая версия ${VER}, но автоматическое обновление отключено.
 .
 Для включения автоматического обновления в будущем вручную выполните команду «sudo dpkg-reconfigure ${PKGI}».

Template: libdvd-pkg/post-invoke_hook-install
Type: boolean
Default: true
Description: Enable automatic upgrades for ${PKGG}?
 If activated, the APT post-invoke hook takes care of future automatic
 upgrades of ${PKGG} (which may be triggered by new versions of
 ${PKGI}). When updates are available, the hook will launch the
 process of downloading the source, recompiling it, and (if "apt-get check"
 reports no errors) using "dpkg -i" to install the new versions.
 .
 Alternatively, the process can be launched manually by running
 "sudo dpkg-reconfigure ${PKGI}".
Description-cs.UTF-8: Povolit automatickou instalaci aktualizací pro ${PKGG}?
 Je-li aktivována, háček APT post-invoke se u ${PKGG} postará o budoucí instalaci aktualizací (které mohou být vyvolány novými verzemi ${PKGI}). Když jsou dostupné aktualizace, spustí tento háček proces stahování zdroje, jeho překompilování, a (pokud "apt-get check" nenahlásí žádné chyby) k instalaci nových verzí použije "dpkg -i".
 .
 Nebo lze proces spustit ručně spuštěním příkazu "sudo dpkg-reconfigure ${PKGI}".
Description-da.UTF-8: Aktiver automatiske opgraderinger for ${PKGG}?
 Hvis aktiveret så tager APT post-invoke-krogen sig af fremtidige automatiske opgraderinger af ${PKGG} (som kan blive udløst af nye versioner af ${PKGI}). Når opdateringer er tilgængelige, vil krogen starte processen med at hente kilden, kompilere den igen og (hvis »apt-get check« rapporterer ingen fejl) og bruge »dpkg -i« for at installere de nye versioner.
 .
 Alternativt kan processen startes manuelt ved at køre »sudo dpkg-reconfigure ${PKGI}«.
Description-de.UTF-8: Sollen automatische Upgrades für ${PKGG} eingeschaltet werden?
 Falls aktiviert, kümmert sich der APT-Hook nach dem Aufruf um automatische Upgrades von ${PKGG} (die durch neue Versionen von ${PKGI} ausgelöst werden können). Wenn Aktualisierungen verfügbar sind, wird der Hook das Herunterladen der Quellen in Gang setzen, sie erneut kompilieren und (falls »apt-get check« keine Fehler zurückgibt) die neuen Versionen mittels »dpkg -i« installieren.
 .
 Alternativ kann der Prozess manuell durch Ausführen von »sudo dpkg-reconfigure ${PKGI}« gestartet werden.
Description-fr.UTF-8: Faut-il activer les mises à niveau automatiques pour ${PKGG} ?
 Si il est activé, le déclencheur après invocation d'APT prend en charge les montées en version futures de ${PKGG} (qui peuvent être déclenchées pour les nouvelles versions de ${PKGI}). Lorsque des mises à jour sont disponibles, le déclencheur lancera le processus de téléchargement de la source, la recompilera, et (si « apt-get check » ne renvoie pas d'erreurs) utilisera dpkg -i pour l'installer.
 .
 Sinon, le processus peut être lancé manuellement en utilisant la commande « sudo dpkg-reconfigure ${PKGI} ».
Description-it.UTF-8: Abilitare gli aggiornamenti automatici per ${PKGG}?
 Se attivati, l'hook post-invoke di APT si occuperà degli aggiornamenti automatici futuri di ${PKGG} (che possono essere causati da nuove versioni di ${PKGI}). Quando sono disponibili aggiornamenti, l'hook avvia il processo di scaricamento dei sorgenti, della loro ricompilazione e (se «apt-get check» non riporta errori) di esecuzione di «dpkg -i» per installare le nuove versioni.
 .
 In alternativa il processo può essere avviato a mano eseguendo «sudo dpkg-reconfigure ${PKGI}».
Description-nl.UTF-8: Het automatisch opwaarderen van ${PKGG} aanzetten?
 Indien dit aangezet wordt, zal de APT-uitbreiding post-invoke in de toekomst zorg dragen voor het automatisch opwaarderen van ${PKGG} (wat uitgelokt kan worden door een nieuwere versie van ${PKGI}). Indien er een opwaardering mogelijk is, zal die uitbreiding het downloaden van de broncode in gang zetten, ze compileren en (indien "apt-get check" geen fout meldt) de nieuwere versies installeren met de opdracht "dpkg -i".
 .
 Anderzijds kan dit proces ook handmatig gestart worden via het uitvoeren van de opdracht "sudo dpkg-reconfigure ${PKGI}".
Description-pt.UTF-8: Activar as actualizações automáticas para ${PKGG}?
 Se activado, o hook APT post-invoke toma conta das actualizações automáticas futuras de ${PKGG} (o que pode ser despoletado por novas versões de ${PKGI}). Quando houver actualizações disponíveis, o hook irá lançar o processo de descarregar a fonte, compilá-la, e (se o "apt-get check" não reportar erros) usar "dpkg -i" para instalar as novas versões.
 .
 Em alternativa, o processo pode ser lançado manualmente ao correr "sudo dpkg-reconfigure ${PKGI}".
Description-ru.UTF-8: Включить автоматическое обновление ${PKGG}?
 При включении, в дальнейшем после установочный обработчик APT будет выполнять автоматическое обновление ${PKGG} (будет запускаться при появлении новых версий ${PKGI}). При появлении обновления, обработчик запустит процесс скачивания исходного кода, его компиляции и установки новой версии в помощью «dpkg -i» (если «apt-get check» не вернёт ошибок).
 .
 Или же процесс можно запускать вручную, выполняя «sudo dpkg-reconfigure ${PKGI}».

Template: libdvd-pkg/post-invoke_hook-remove
Type: boolean
Default: false
Description: Disable automatic upgrades for ${PKGG}?
 If activated, the APT post-invoke hook takes care of future automatic
 upgrades of ${PKGG} (which may be triggered by new versions of
 ${PKGI}). When updates are available, the hook will launch the
 process of downloading the source, recompiling it, and (if "apt-get check"
 reports no errors) using "dpkg -i" to install the new versions.
 .
 Alternatively, the process can be launched manually by running
 "sudo dpkg-reconfigure ${PKGI}".
Description-cs.UTF-8: Zakázat automatickou instalaci aktualizací pro ${PKGG}?
 Je-li aktivována, háček APT post-invoke se u ${PKGG} postará o budoucí instalaci aktualizací (které mohou být vyvolány novými verzemi ${PKGI}). Když jsou dostupné aktualizace, spustí tento háček proces stahování zdroje, jeho překompilování, a (pokud "apt-get check" nenahlásí žádné chyby) k instalaci nových verzí použije "dpkg -i".
 .
 Nebo lze proces spustit ručně spuštěním příkazu "sudo dpkg-reconfigure ${PKGI}".
Description-da.UTF-8: Deaktiver automatiske opgraderinger for ${PKGG}?
 Hvis aktiveret så tager APT post-invoke-krogen sig af fremtidige automatiske opgraderinger af ${PKGG} (som kan blive udløst af nye versioner af ${PKGI}). Når opdateringer er tilgængelige, vil krogen starte processen med at hente kilden, kompilere den igen og (hvis »apt-get check« rapporterer ingen fejl) og bruge »dpkg -i« for at installere de nye versioner.
 .
 Alternativt kan processen startes manuelt ved at køre »sudo dpkg-reconfigure ${PKGI}«.
Description-de.UTF-8: Sollen automatische Upgrades für ${PKGG} ausgeschaltet werden?
 Falls aktiviert, kümmert sich der APT-Hook nach dem Aufruf um automatische Upgrades von ${PKGG} (die durch neue Versionen von ${PKGI} ausgelöst werden können). Wenn Aktualisierungen verfügbar sind, wird der Hook das Herunterladen der Quellen in Gang setzen, sie erneut kompilieren und (falls »apt-get check« keine Fehler zurückgibt) die neuen Versionen mittels »dpkg -i« installieren.
 .
 Alternativ kann der Prozess manuell durch Ausführen von »sudo dpkg-reconfigure ${PKGI}« gestartet werden.
Description-fr.UTF-8: Faut-il désactiver les mises à niveau automatiques pour ${PKGG} ?
 Si il est activé, le déclencheur après invocation d'APT prend en charge les montées en version futures de ${PKGG} (qui peuvent être déclenchées pour les nouvelles versions de ${PKGI}). Lorsque des mises à jour sont disponibles, le déclencheur lancera le processus de téléchargement de la source, la recompilera, et (si « apt-get check » ne renvoie pas d'erreurs) utilisera dpkg -i pour l'installer.
 .
 Sinon, le processus peut être lancé manuellement en utilisant la commande « sudo dpkg-reconfigure ${PKGI} ».
Description-it.UTF-8: Disabilitare gli aggiornamenti automatici per ${PKGG}?
 Se attivati, l'hook post-invoke di APT si occuperà degli aggiornamenti automatici futuri di ${PKGG} (che possono essere causati da nuove versioni di ${PKGI}). Quando sono disponibili aggiornamenti, l'hook avvia il processo di scaricamento dei sorgenti, della loro ricompilazione e (se «apt-get check» non riporta errori) di esecuzione di «dpkg -i» per installare le nuove versioni.
 .
 In alternativa il processo può essere avviato a mano eseguendo «sudo dpkg-reconfigure ${PKGI}».
Description-nl.UTF-8: Het automatisch opwaarderen van ${PKGG} uitzetten?
 Indien dit aangezet wordt, zal de APT-uitbreiding post-invoke in de toekomst zorg dragen voor het automatisch opwaarderen van ${PKGG} (wat uitgelokt kan worden door een nieuwere versie van ${PKGI}). Indien er een opwaardering mogelijk is, zal die uitbreiding het downloaden van de broncode in gang zetten, ze compileren en (indien "apt-get check" geen fout meldt) de nieuwere versies installeren met de opdracht "dpkg -i".
 .
 Anderzijds kan dit proces ook handmatig gestart worden via het uitvoeren van de opdracht "sudo dpkg-reconfigure ${PKGI}".
Description-pt.UTF-8: Desactivar as actualizações automáticas para ${PKGG}?
 Se activado, o hook APT post-invoke toma conta das actualizações automáticas futuras de ${PKGG} (o que pode ser despoletado por novas versões de ${PKGI}). Quando houver actualizações disponíveis, o hook irá lançar o processo de descarregar a fonte, compilá-la, e (se o "apt-get check" não reportar erros) usar "dpkg -i" para instalar as novas versões.
 .
 Em alternativa, o processo pode ser lançado manualmente ao correr "sudo dpkg-reconfigure ${PKGI}".
Description-ru.UTF-8: Отключить автоматическое обновление ${PKGG}?
 При включении, в дальнейшем после установочный обработчик APT будет выполнять автоматическое обновление ${PKGG} (будет запускаться при появлении новых версий ${PKGI}). При появлении обновления, обработчик запустит процесс скачивания исходного кода, его компиляции и установки новой версии в помощью «dpkg -i» (если «apt-get check» не вернёт ошибок).
 .
 Или же процесс можно запускать вручную, выполняя «sudo dpkg-reconfigure ${PKGI}».
