
Template: shared/slapos/master_url
Type: string
Description: SlapOS master node URL:
Description-cs.UTF-8: URL hlavního uzlu SlapOS:
Description-da.UTF-8: SlapOS' adresse for overknude:
Description-de.UTF-8: URL des SlapOS-Masterknotens:
Description-es.UTF-8: URL del nodo maestro de SlapOS:
Description-fr.UTF-8: Adresse du nœud maître SlapOS :
Description-it.UTF-8: URL del nodo master di SlapOS:
Description-ja.UTF-8: SlapOS のマスターノード URL:
Description-nl.UTF-8: SlapOS hoofdknoop-URL:
Description-pl.UTF-8: URL głównego węzła SlapOS:
Description-pt.UTF-8: URL do nó principal do SlapOS:
Description-ru.UTF-8: URL главного узла SlapOS:
Description-sv.UTF-8: URL till SlapOS huvudnod:

Template: shared/slapos/master_url_with_ssl_note
Type: note
Description: Master node key and certificate mandatory
 You used an HTTPS URL for the SlapOS master node, so the corresponding
 certificate must be placed in /etc/slapos/ssl/slapos.crt, and the key
 in /etc/slapos/ssl/slapos.key, readable only to root.
Description-cs.UTF-8: Klíč hlavního uzlu a certifikát je povinný
 Pro hlavní uzel SlapOS jste použili URL s HTTPS, takže v /etc/slapos/ssl/slapos.crt musí být umístěn příslušný certifikát a v /etc/slapos/ssl/slapos.key klíč, čitelný pouze uživatelem root.
Description-da.UTF-8: Nøgle for overknude og certifikat er krævet
 Du brugte en HTTPS URL for SlapOS' overknude, så det tilsvarende certifikat skal placeres i /etc/slapos/ssl/slapos.crt, og nøglen i /etc/slapos/ssl/slapos.key, kun læsbart for administrator (root).
Description-de.UTF-8: Pflichtschlüssel und -zertifikat des Masterknotens
 Sie haben eine HTTPS-URL für den SlapOS-Masterknoten verwandt, daher muss das zugehörige Zertifikat in /etc/slapos/ssl/slapos.crt und der Schlüssel nur für Root lesbar in /etc/slapos/ssl/slapos.key platziert werden.
Description-es.UTF-8: La clave y el certificado del nodo maestro son obligatorios
 Ha utilizado una URL con HTTPS para el nodo maestro de SlapOS, de modo que se deben instalar el certificado correspondiente en «/etc/slapos/ssl/slapos.crt», y la clave en «/etc/slapos/ssl/slapos.key». Estos ficheros deberían estar configurados con acceso de lectura sólo para el administrador.
Description-fr.UTF-8: Clé et certificat obligatoires pour le nœud maître
 Une adresse HTTPS a été choisie pour le nœud maître de SlapOS. Dans ce cas, le certificat correspondant doit être mis dans /etc/slapos/ssl/slapos.crt, et la clé dans /etc/slapos/ssl/slapos.key uniquement lisible par le superutilisateur.
Description-it.UTF-8: Chiave e certificato obbligatori per il nodo master
 È stato usato un URL HTTPS per il nodo master di SlapOS, perciò il certificato corrispondente deve essere messo in /etc/slapos/ssl/slapos.crt e la chiave in /etc/slapos/ssl/slapos.key, entrambi leggibili solo da root.
Description-ja.UTF-8: マスターノード鍵と証明書が必須です
 HTTPS URL をマスターノードとして入力したので、対応する鍵と証明書を /etc/slapos/ssl/slapos.key と /etc/slapos/ssl/slapos.crt に置いて、root のみが読み取れるようにしておく必要があります。
Description-nl.UTF-8: Sleutel en certificaat van de hoofdknoop verplicht
 U heeft een HTTPS-URL opgegeven voor de SlapOS hoofdknoop; het bijbehorende certificaat moet in /etc/slapos/ssl/slapos.crt worden gezet en de sleutel in /etc/slapos/ssl/slapos.key. De sleutel mag alleen leesbaar zijn voor de beheerder.
Description-pl.UTF-8: Klucz i certyfikat głównego węzła jest obowiązkowy
 Używany jest URL HTTPS do głównego węzła SlapOS, w związku z czym odpowiedni certyfikat musi zostać umieszczony w /etc/slapos/ssl/slapos.crt, a klucz w etc/slapos/ssl/slapos.key (do odczytu tylko dla roota).
Description-pt.UTF-8: Chave e certificado obrigatório do nó principal
 Usou um URL HTTPS para o nó principal do SlapOS, deste modo o certificado correspondente deverá ser colocado em /etc/slapos/ssl/slapos.crt, e a chave em /etc/slapos/ssl/slapos.key, com acesso de leitura apenas ao root.
Description-ru.UTF-8: Сертификат и ключ главного узла
 Для доступа к главному узлу SlapOS используется HTTPS URL, поэтому в /etc/slapos/ssl/slapos.crt должен быть соответствующий сертификат, а в /etc/slapos/ssl/slapos.key — ключ (доступны только root).
Description-sv.UTF-8: Huvudnod-nyckel och certifikat behövs
 Du använde en HTTPS URL för SlapOS huvudnod, motsvarande certifikat måste placeras i /etc/slapos/ssl/slapos.crt och nyckeln i /etc/slapos/ssl/slapos.key, dessa ska endast vara läsbara av root.
